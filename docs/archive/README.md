# Archive — finished handoffs

Both files in here describe work that **shipped**. They are kept because the
reasoning in them is good and occasionally still answers a question ("why does
`monthScope` exist?", "why did the review console lose half its UI?"). They
are not instructions, and nothing in them is owed.

| File | Was | Now |
|---|---|---|
| `HANDOFF-month-scope.md` | the `monthScope` axis, mid-build | shipped — `src/domain/months.ts` |
| `HANDOFF-console-rebuild.md` | the review-console cut, mid-refactor | shipped — `src/review/` |

**They both used to name the repo as `/Users/Sergey/Code/paycheck-guard`.**
That path is wrong and has already cost one session: the repo is the iCloud
path in `CLAUDE.md`. The strings have been corrected here rather than left to
mislead the next reader.

The live handoff is `docs/HANDOFF-2026-09-03.md`. When it too is finished,
it comes here.
